{
  "name": "playwright-ts-env-setup",
  "version": "1.0.0",
  "description": "Playwright Environment Setup and Configuration",
  "main": "index.js",
  "scripts": {
    "env:dev": "cross-env test_env=dev npx playwright test",
    "env:stg": "cross-env test_env=stg npx playwright test",
    "env:prod": "cross-env test_env=prod npx playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.40.1",
    "@types/node": "^20.10.5",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
